import 'package:latlong2/latlong.dart';

class AppConstants{
  static const String urlTemplate = "https://api.mapbox.com/styles/v1/{id}/tiles/{z}/{x}/{y}?access_token={accessToken}";
  static const myLocation = LatLng(-0.2320341, -78.5088358);
}